<Window x:Class="BubbleStart.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BubbleStart.Views"
        Loaded="MainWindow_OnLoaded"
        mc:Ignorable="d"
        FontSize="13"
        Height="795"
        Language="el-Gr"
        Width="1366"
        Closed="Window_Closed"
        xmlns:viewmodels="clr-namespace:BubbleStart.ViewModels"
        xmlns:views="clr-namespace:BubbleStart.Views"
        d:DataContext="{d:DesignInstance viewmodels:MainViewModel}"
        WindowStartupLocation="CenterScreen">
    <Window.IsEnabled>
        <Binding Path="BindableProperty"
                 Source="{StaticResource MessageListener}" />
    </Window.IsEnabled>
    <Window.Visibility>
        <Binding Path="Visibility" />
    </Window.Visibility>
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:LoginViewModel}">
            <views:Login_UserControl />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:MainUserControl_ViewModel}">
            <views:Main_UserControl />
        </DataTemplate>
    </Window.Resources>

    <ContentControl Content="{Binding SelectedViewmodel}"
                    Grid.Row="1" />
</Window>