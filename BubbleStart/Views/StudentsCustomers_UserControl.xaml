<UserControl x:Class="BubbleStart.Views.StudentsCustomers_UserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:Converters="clr-namespace:BubbleStart.Converters"
             xmlns:viewmodels="clr-namespace:BubbleStart.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance {x:Type viewmodels:StudentsCustomers_ViewModel}}"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <Converters:RowToIndexConverter x:Key="RowToIndexConverter" />
        <Converters:BoolToGreekStringConverter x:Key="BoolToGreekStringConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
            <TextBox Grid.Row="0"
                     FontSize="15"
                     Margin="5"
                     Width="300"
                     Text="{Binding SearchTerm, Delay=100, UpdateSourceTrigger=PropertyChanged}" />
            <CheckBox IsChecked="{Binding ActiveOnly}" Content="Μόνο ενεργοί" Margin="5" VerticalAlignment="Center"/>
            <Button Content="Προβολή"
                    VerticalAlignment="Center"
                    IsDefault="True"
                    Margin="5"
                    Command="{Binding ShowStudentsCommand}" />
        </StackPanel>
        <DataGrid ItemsSource="{Binding StudentsCollectionView}"
                  Grid.Row="1"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  VirtualizingPanel.VirtualizationMode="Standard"
                  IsReadOnly="True"
                  CanUserDeleteRows="False"
                  AutoGenerateColumns="False">
            <DataGrid.RowStyle>
                <Style TargetType="{x:Type DataGridRow}">
                    <EventSetter Event="MouseDoubleClick"
                                 Handler="ListView_MouseDoubleClick" />
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="#"
                                   
                                    Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Converter={StaticResource  RowToIndexConverter}}" />
                <DataGridTextColumn Header="Όνομα"
                                    Width="110"
                                    SortMemberPath="IsActiveColorq"
                                    Binding="{Binding Name}" />
                <DataGridTextColumn Header="Επίθετο"
                                    Width="110"
                                    Binding="{Binding SureName}" />
                <DataGridTextColumn Header="Ηλικ."
                                    Width="35"
                                    Binding="{Binding Age}" />
                <DataGridTextColumn Header="Tel"
                                    Width="110"
                                    FontSize="15"
                                    Binding="{Binding Tel}" />
                <DataGridTextColumn Header="Ενεργός"
                                    Width="60"
                                    FontSize="15"
                                    Binding="{Binding Enabled,Converter={StaticResource BoolToGreekStringConverter}}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>